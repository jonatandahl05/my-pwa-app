{
  "name": "my-pwa-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "api": "node src/Caching/server.js",
    "dev:all": "concurrently -k -n \"vite,api\" -c \"blue,green\" \"npm run dev\" \"npm run api\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "vite": "^7.2.4"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.5"
  }
}
